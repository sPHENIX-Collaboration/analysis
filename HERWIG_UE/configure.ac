AC_INIT([herwigstaruecheck],[1.00])
AC_CONFIG_SRCDIR([configure.ac])

AM_INIT_AUTOMAKE
AC_PROG_CXX(CC g++)

LT_INIT([disable-static])

dnl no dictionary needed: only standard ROOT objects are streamed

AC_CONFIG_FILES([Makefile])
AC_OUTPUT
