To compile macro on RCF (with sPHENIX environment initialized) run:

g++ `root-config --cflags --glibs` -L$OFFLINE_MAIN/lib -I$OFFLINE_MAIN/include -leicsmear eic_sphenix_dis_fillbins.C -o eic_sphenix_dis_filbins

This daves a lot of time when running the code!
